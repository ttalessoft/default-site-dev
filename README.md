# default-site-dev

Ambiente padrão preconfigurado para desenvolvimento de sites web

STEP BY STEP

1 - Copiar o diretório para sua máquina;
2 - Instalar as dependências pelo NPM com o comando:
npm install
